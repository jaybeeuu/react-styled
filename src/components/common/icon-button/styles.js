// import * as mixins from "../../styles/mixins";

// @keyframes spin {
//     from { transform:rotate(0deg); }
//     to { transform:rotate(360deg); }
// }

export const icon = {
  display: "inline-block"
};

// &:hover > &__icon {
//     animation: spin $transition-duration-complex ease;
// }